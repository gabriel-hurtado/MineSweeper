<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gabriel.minesweeper" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainMenu"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HelpActivity"
            android:label="@string/title_activity_help" />
        <activity
            android:name=".GameStarterActivity"
            android:label="@string/title_activity_game_starter" />
        <activity
            android:name=".GameActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/title_activity_game" />
        <activity
            android:name=".ResultGameActivity"
            android:label="@string/title_activity_result_game" />
        <activity
            android:name=".AccesDBActivity"
            android:label="@string/title_activity_consult" />
        <activity
            android:name=".DetailRegisterActivity"
            android:label="@string/title_activity_detail_register" >
        </activity>
    </application>

</manifest>
